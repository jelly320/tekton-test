# 关联triggertemplate和triggerbinding
apiVersion: triggers.tekton.dev/v1alpha1 
kind: EventListener
metadata:
  name: trigger-taghook-eventlistener
spec:
  serviceAccountName: tekton-triggers-sa
  triggers:
    - name: trigger
      interceptors:
          - gitlab:
              # 配置gitlab的信息和Token的key
              secretRef:
                secretName: gitlab-token 
                secretKey: secretToken 
              eventTypes:
                - Tag Push Hook 
      bindings:
        - ref: trigger-taghook-triggerbinding
      template:
        ref: trigger-taghook-triggertemplate
